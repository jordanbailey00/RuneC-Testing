id = "collision_tiles"
format = "binary"
path = "data/regions/world.collision-tiles.indexed.bin"
magic_label = "CTPI"
magic_u32 = "0x49505443"
current_version = 1
accepted_versions = [1]
endianness = "little"
header = [
  "u32 magic",
  "u32 version",
  "u32 row_count",
  "u32 record_size",
  "u32 occupied_mapsquare_count",
  "u32 plane_0_count",
  "u32 plane_1_count",
  "u32 plane_2_count",
  "u32 plane_3_count",
]
producer = "tools/export_collision_tiles.py"
consumers = ["rc-core/collision.c", "tools/pack_runtime_data.py"]
source_dataset = "b237 cache terrain and object collision decoded by RuneC cache tools"
index_entry_fields = ["u32 first_record", "u32 record_count"]
row_fields = [
  "u8 local_x",
  "u8 local_y",
  "u8 plane",
  "u8 pad",
  "u32 collision_flags",
]
compatibility = "Loader accepts exactly current_version; CTIL v1 is superseded and not accepted."
